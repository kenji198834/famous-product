<main class="main">
  <div class="inner">
    <%# ログインしているときは以下を表示する %>
      <% if user_signed_in? %>
      <div class="greeting">
        こんにちは、 
        <%= link_to "#{current_user.nickname}さん", user_path(current_user.id), class: :greeting__link %>
      </div>
      <% end %>
    <%# // ログインしているときは上記を表示する %>
    <h1>四国地方</h1>
    <p><%= link_to "徳島県", posts_path %></p>
    <p><%= link_to "香川県", posts_path %></p>
    <div class="card__wrapper">
    </div>
  </div>
</main>